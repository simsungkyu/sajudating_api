# Task plan files (runLoopForSaju.sh)

Task plan files are created here by **Phase 1** of `runLoopForSaju.sh`, with creation-time filenames:

- `task_YYYY-MM-DDTHHMMSS.md`

**Phase 2** executes the plan in that file. One cycle = one plan file + one execute run.
