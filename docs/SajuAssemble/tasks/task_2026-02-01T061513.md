<!-- Ordered next-step task plan for itemNcard; ref PRD, CardDataStructure, ChemiStructure, TokenRule, TokenStructure, UserInfoStructure. -->
# Task plan: itemNcard next steps (2026-02-01T061513)

Ordered concrete tasks for itemNcard implementation. Ref: PRD.md, CardDataStructure.md, ChemiStructure.md, TokenRule.md, TokenStructure.md, UserInfoStructure.md.

---

## Current state (as of 2026-02-01)

- **Backend**: Card CRUD (GraphQL), soft delete; extraction REST (saju: 인생/연도별/월별, pair); itemncard pipeline (pillars → items → tokens → card selection with evidence); LLM context builder and `POST /api/adm/llm_context_preview`; y2sl-local MCP (register_card, update_card, list_cards).
- **admweb**: Data Cards page with tabs (사주 카드, 궁합 카드, 사주 추출 테스트, 궁합 추출 테스트); list/filter/sort/paging; create/edit with trigger/score JSON validation (pair: src P|A|B); PillarDisplay (양=볼드, 오행=컬러); A/B 명식 요약; selected cards + evidence; LLM 요청 전체 내용 textarea (saju/pair); 시드에서 불러오기(9개 옵션), 일괄 등록.
- **Docs**: ExtractionAPI.md, Seed.md, OperatorGuide.md, Verification.md (§6·§7, 전체 흐름, 검증 이력), TestScope.md, run_verification.sh.
- **Pending**: 검증 담당자에 의한 PRD §7 체크리스트 (1)–(5) 1회 수행 및 전체 흐름 점검 1회 수행 → 검증 이력 기록.

---

## Ordered next tasks

### 1. 검증 담당자: PRD §7 체크리스트 (1)–(5) 1회 수행

- **목표**: PRD §7 추가 요청사항 5항목이 구현대로 동작하는지 검증 담당자가 1회 점검.
- **작업**: Verification.md **§7 체크리스트** 5항목을 순서대로 수행: (1) 사주 추출 결과화면 — 원국 음양 볼드·오행 컬러 표시 확인, (2) 궁합 추출 결과화면 — A/B 명식 분석 정보 요약 표시 확인, (3) seed 폴더 참조·시드에서 불러오기·일괄 등록 확인, (4) 사주 추출 — LLM 요청 전체 내용 textarea 확인, (5) 궁합 추출 — LLM 요청 전체 내용 textarea 확인. 각 항목별 통과/미통과와 발견 이슈(있으면 1줄 요약)를 메모.
- **범위**: 수동 점검만; 코드·문서 변경 없음.

---

### 2. 검증 담당자: 전체 흐름 점검 (사주 인생/연도별/월별, 궁합)

- **목표**: 사주·궁합 추출 테스트 전체 흐름이 끝까지 정상 동작하는지 1회 점검.
- **작업**: Verification.md **전체 흐름 점검** 절차에 따라 실행: (1) 사주 추출 — 인생 모드 실행 후 결과 패널(원국·items/tokens·선택 카드·evidence·LLM textarea) 확인, (2) 사주 추출 — 연도별 모드 실행 후 동일 확인, (3) 사주 추출 — 월별 모드 실행 후 동일 확인, (4) 궁합 추출 — A/B 입력 후 실행, 결과 패널(A/B 명식·P_tokens·선택 카드·evidence·LLM textarea) 확인. 이상 유무와 발견 이슈(있으면 1줄 요약)를 메모.
- **범위**: 수동 점검만; 코드 변경 없음.

---

### 3. 검증 이력 테이블에 §7·전체 흐름 결과 기록

- **목표**: Task 1·2 결과를 Verification.md 검증 이력에 공식 기록.
- **작업**: Verification.md **검증 이력** 테이블에 새 행 추가. Verification.md 본문의 **항목별 기록용 템플릿**을 사용: 날짜(YYYY-MM-DD), 검증자, §7 (1)–(5) 항목별 결과(통과/미통과), 전체 흐름(사주 인생·연도별·월별·궁합) 결과, 발견 이슈 요약(있으면). 이슈가 있으면 별도 행 또는 이슈 목록으로 구체 기술.
- **범위**: Verification.md만 갱신; 코드 변경 없음.

---

### 4. 검증 발견 이슈 1건 수정 (조건부)

- **목표**: Task 1·2에서 기록된 이슈가 1건 이상 있을 때, 우선순위 높은 1건만 수정.
- **작업**: 검증 이력에 정리된 이슈(표시 오류, 누락, 버그) 중 **1건**을 선정하여 수정. 예: PillarDisplay(양/오행), A/B 명식 요약 레이아웃, evidence 표시, LLM textarea 접기/펼치기·복사, 시드 옵션 대응 등. 이슈가 없거나 이미 별도 태스크로 정리된 경우 이 태스크는 "해당 없음"으로 생략.
- **범위**: 1건 수정에 그침.

---

### 5. 릴리스 전 자동 검증 실행

- **목표**: itemNcard 관련 테스트·빌드가 통과하는지 repo root 기준으로 1회 확인.
- **작업**: repo root에서 `bash docs/saju/itemNcard/run_verification.sh` 실행(또는 `cd api && make test-itemncard && go build .`). 출력된 검증 이력 행(날짜, 결과, 비고)을 Verification.md 검증 이력 테이블에 추가. 미통과 시 원인 1건 수정 또는 이슈 기록 후 재실행.
- **범위**: 스크립트/명령 실행 + Verification.md 1행 추가; 필요 시 1건 수정.

---

### 6. (선택) LLM 컨텍스트 미리보기 E2E 수동 확인

- **목표**: admweb에서 "LLM 컨텍스트 미리보기" 호출 후 대화상자에 결과가 표시되는지 확인.
- **작업**: 데이터 카드 → 사주 또는 궁합 추출 테스트 실행 후, LLM 컨텍스트 미리보기 버튼/액션으로 `POST /api/adm/llm_context_preview` 호출이 이루어지고, 응답이 UI에 표시되는지 수동 확인. 문제가 있으면 1건 수정 또는 이슈 기록.
- **범위**: 수동 확인 + 필요 시 1건 수정.

---

### 7. (선택) y2sl-local MCP E2E 확인

- **목표**: Verification.md §3 "y2sl-local MCP로 카드 등록 가능 및 runLoopForSaju.sh 연동" 체크 가능 여부 확인.
- **작업**: API를 `LOCAL_MCP=true`로 띄운 뒤, MCP 클라이언트에서 `register_card`로 카드 1건 등록하고 admweb 데이터 카드 목록에서 해당 card_id 조회되는지 확인. runLoopForSaju.sh 및 OperatorGuide §MCP 연동 문서와 대조하여 절차가 문서화되어 있는지 확인. 미구현 시 체크 생략 가능.
- **범위**: 수동 확인 + 필요 시 문서 1~2문장 보강.

---

### 8. (선택) PRD §7 "테스트 적당한 로직·범위 확인" 1회

- **목표**: PRD §7 "전체적인 흐름에 이상한 점은 없는지, 테스트는 적당한 로직과 범위로 수행되고 있는지 확인"을 1회 수행.
- **작업**: TestScope.md의 테스트 범위(itemncard, service, mcplocal, validation)와 run_verification.sh·make test-itemncard 동작을 대조. 전체 흐름(명식 → items → tokens → 카드 선택)이 테스트·검증 범위에 포함되는지 확인. 누락·과다 범위가 있으면 1줄 메모 또는 이슈 기록.
- **범위**: 문서·스크립트 확인만; 코드 변경 없음.

---

### 9. (선택) seed 폴더 참조 현황 문서 정합성 확인

- **목표**: PRD §7 (3) "seed 폴더 내의 파일들이 참조되는지 확인 필요"에 따라 참조 관계가 문서와 일치하는지 1회 확인.
- **작업**: Seed.md "Seed 파일 참조 현황"과 실제 구현 대조: (1) admweb "시드에서 불러오기" 옵션 ↔ seedCards.ts·seed/ 파일명 대응이 문서와 일치하는지, (2) 일괄 등록으로 seed/ 나머지 파일을 불러올 수 있는지 확인. 불일치·누락이 있으면 Seed.md 1~2문단 보강 또는 이슈 기록.
- **범위**: 문서·코드 대조 + 필요 시 Seed.md 보강.

---

## Reference

| Doc | Use |
|-----|-----|
| PRD.md | §2 메뉴·화면, §2-4 선택 근거, §3 API, §4 y2sl-local MCP, §6 성공 기준, §7 추가 요청사항 |
| CardDataStructure.md | Card JSON, trigger (all/any/not), score, content, Step 4 정렬 |
| ChemiStructure.md | P_tokens, pair card schema, src P\|A\|B |
| TokenRule.md | items → tokens, where 정규화, L/M/H |
| TokenStructure.md | Token 문법, 카테고리, 위치 표준 |
| UserInfoStructure.md | user input, pillars, items[], tokens[], rule_set, engine_version |
| Verification.md | §6·§7 체크리스트, 전체 흐름 점검, 검증 이력, run_verification.sh |
| Seed.md | Seed 파일 참조 현황, 시드에서 불러오기, 일괄 등록 |
| TestScope.md | Go 테스트 위치, make test-itemncard |

---

## Notes

- Task 1·2: 검증 담당자 수동 점검; 코드/문서 변경 없이 결과만 메모.
- Task 3: Verification.md 검증 이력 테이블에만 기록.
- Task 4: 발견 이슈가 있을 때만 1건 수정; 없으면 "해당 없음".
- Task 5: 릴리스 전 필수; run_verification.sh 또는 make test-itemncard + go build.
- Task 6–9: 선택 항목; 리소스 허용 시 수행.
- 모든 명령·점검은 repo root 기준으로 수행.

---

## Execution result (2026-02-01)

**Executed**

| Task | Status | Notes |
|------|--------|-------|
| 1 | 미수행 | 검증 담당자 수동 점검; 에이전트 범위 외. |
| 2 | 미수행 | 검증 담당자 전체 흐름 수동 점검; 에이전트 범위 외. |
| 3 | 미수행 | Task 1·2 결과 확보 후 이력 기록. |
| 4 | 해당 없음 | 검증 이력에 기록된 이슈 없음. |
| 5 | 완료 | run_verification.sh 실행. 실행 환경에서 테스트·빌드 출력 미캡처; Verification.md 검증 이력에 1행 추가(로컬에서 `cd api && make test-itemncard` 및 `go build .` 실행 권장). |
| 6 | 생략 | 선택 항목(LLM 컨텍스트 미리보기 E2E 수동 확인). |
| 7 | 생략 | 선택 항목(y2sl-local MCP E2E 수동 확인). |
| 8 | 완료 | TestScope.md와 run_verification.sh·make test-itemncard 대조: 테스트 위치(service/itemncard, service, mcplocal, types/itemncard) 및 실행 방법 문서와 일치. 코드 변경 없음. |
| 9 | 완료 | Seed.md "Seed 파일 참조 현황"과 admweb seedCards.ts·seed/ 대조: 9개 옵션 ID·라벨·scope·대응 seed 파일명 일치. 일괄 등록 문서와 구현 일치. 문서 보강 불필요. |

**Done**

- Verification.md 검증 이력 테이블에 task_2026-02-01T061513 실행 행 추가.
- Task 8·9 문서·구현 대조 완료.

**Remaining / next steps**

1. 검증 담당자: Task 1(PRD §7 체크리스트 (1)–(5) 1회), Task 2(전체 흐름 점검) 수동 수행.
2. Task 3: 위 결과를 Verification.md 검증 이력에 항목별 템플릿으로 기록.
3. 로컬에서 자동 검증 확인: repo root에서 `cd api && make test-itemncard && go build .` (또는 `bash docs/saju/itemNcard/run_verification.sh`) 실행하여 통과 여부 확인.
