---
description: Internal service name for saju/compatibility card selection and recombination (SajuAssemble)
alwaysApply: true
---

# SajuAssemble (사주어셈블) — 내부 서비스 명칭

이 프로젝트에서 **사주·궁합 데이터 카드의 선별·재조합 로직**에 대한 **내부 서비스 명칭**은 다음을 사용한다.

- **영문**: **SajuAssemble**
- **한글**: **사주어셈블**

해당 로직은 명식(원국/items/tokens) 또는 궁합(P_tokens) 데이터를 **구분·선별**하고, 트리거 매칭으로 **카드로 재조합**하는 흐름을 의미한다. 코드·모듈·GraphQL·API·문서 등 내부에서 이 기능을 지칭할 때 **SajuAssemble** / **사주어셈블**을 사용한다.

**예**: `SajuAssembleService`, `saju_assemble`, `saju-assemble`, 주석/PRD에서 "SajuAssemble(사주어셈블)".
